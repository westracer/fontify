/// A class that contains offset pointers which need be recalculated after data change
abstract class CalculatableOffsets {
  /// Recalculate offset pointers and change their values
  void recalculateOffsets() {}
}
